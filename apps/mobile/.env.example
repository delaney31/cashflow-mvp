# Public variables only (embedded in the client bundle).
# See apps/mobile/src/api/env.ts

# API base URL including /v1 prefix
# EXPO_PUBLIC_API_URL=http://localhost:3000/v1

# Optional dev-only: JWT from POST /v1/auth/login (same user as db seed: usr_mock_mvp_001)
# EXPO_PUBLIC_API_TOKEN=
